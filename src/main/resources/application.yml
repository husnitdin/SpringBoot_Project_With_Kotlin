spring:
    datasource:
        driver-class-name: org.h2.Driver
        password: password
        url: jdbc:h2:file:./data/testdb
        username: name
    sql:
        init:
            mode: always
            schema-locations: classpath:schema.sql
